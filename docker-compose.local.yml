services:
  toolify:
    build: .
    container_name: toolify
    ports:
      - "3006:8000"
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
    networks:
      - network

networks:
  network:
    name: apps_default
    external: true
